---
phase: 01-core-agent-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/codagent/__init__.py
  - src/codagent/config.py
  - src/codagent/models.py
autonomous: true
requirements:
  - MODL-01
  - MODL-02

must_haves:
  truths:
    - "API keys are loaded from .env file at startup and accessible via config object"
    - "Three model providers (OpenAI, Anthropic, OpenRouter) are registered with correct Pydantic AI model strings"
    - "Default model is configurable via environment variable"
    - "Model registry maps friendly names to provider-prefixed model strings"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, entry point"
      contains: "pydantic-ai-slim"
    - path: "src/codagent/config.py"
      provides: "Settings class with .env loading and runtime mode state"
      exports: ["Settings", "load_settings"]
    - path: "src/codagent/models.py"
      provides: "Model provider registry with friendly name mapping"
      exports: ["MODEL_REGISTRY", "get_model", "list_models"]
  key_links:
    - from: "src/codagent/config.py"
      to: ".env"
      via: "python-dotenv load_dotenv()"
      pattern: "load_dotenv"
    - from: "src/codagent/models.py"
      to: "src/codagent/config.py"
      via: "reads default_model from settings"
      pattern: "settings\\.default_model|DEFAULT_MODEL"
---

<objective>
Set up the project skeleton, dependency management, configuration loading, and model provider registry — the foundation everything else builds on.

Purpose: Every subsequent plan imports from config.py and models.py. This must exist first.
Output: A `uv`-installable Python project with config loading from `.env` and a model registry mapping friendly names to Pydantic AI model strings.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-agent-loop/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton with pyproject.toml, package structure, and config module</name>
  <files>
    pyproject.toml
    .env.example
    src/codagent/__init__.py
    src/codagent/config.py
  </files>
  <action>
    1. Create `pyproject.toml` with:
       - Project name: `codagent`
       - Python requires: `>=3.10`
       - Dependencies: `pydantic-ai-slim[openai,anthropic,openrouter]>=1.63.0`, `python-dotenv>=1.2.1`
       - Build system: hatchling (or setuptools)
       - Entry point: `[project.scripts] codagent = "codagent.main:main"`

    2. Create `src/codagent/__init__.py` with package version string (e.g., `__version__ = "0.1.0"`).

    3. Create `.env.example` with documented environment variables:
       ```
       OPENAI_API_KEY=
       ANTHROPIC_API_KEY=
       OPENROUTER_API_KEY=
       DEFAULT_MODEL=gpt5
       DEFAULT_MODE=approval
       COMMAND_TIMEOUT=120
       ```

    4. Create `src/codagent/config.py`:
       - Import `load_dotenv` from `dotenv` and call it at module level or in `load_settings()`.
       - Define a `Settings` dataclass or simple class with fields:
         - `openai_api_key: str | None`
         - `anthropic_api_key: str | None`
         - `openrouter_api_key: str | None`
         - `default_model: str` (default `"gpt5"`)
         - `mode: str` (default `"approval"`) — mutable at runtime
         - `command_timeout: int` (default `120`)
       - `load_settings()` function reads from `os.environ` (after dotenv loads).
       - Use a module-level `_settings: Settings | None` singleton pattern with a `get_settings()` accessor so other modules can import it.

    Do NOT use Pydantic BaseSettings — keep it simple with a dataclass or plain class. The `mode` field must be mutable (it toggles between "approval" and "yolo" at runtime).
  </action>
  <verify>
    <automated>cd /Users/kshitij/Documents/coding-agent-1 && uv pip install -e ".[dev]" 2>/dev/null || uv pip install -e . && python -c "from codagent.config import load_settings, get_settings; s = load_settings(); print(f'mode={s.mode}, timeout={s.command_timeout}')"</automated>
    <manual>Verify .env.example has all documented keys</manual>
  </verify>
  <done>Project installs cleanly with uv, config loads default settings when no .env exists, Settings object accessible via get_settings()</done>
</task>

<task type="auto">
  <name>Task 2: Create model provider registry with friendly name mapping</name>
  <files>
    src/codagent/models.py
  </files>
  <action>
    Create `src/codagent/models.py` with:

    1. A `MODEL_REGISTRY` dict mapping friendly names to Pydantic AI model strings:
       ```python
       MODEL_REGISTRY: dict[str, str] = {
           "gpt5": "openai:gpt-5",
           "claude": "anthropic:claude-4.5-sonnet",
           "groq": "openrouter:x-ai/grok-code-fast-1",
       }
       ```
       NOTE from research: OpenRouter model names can change. Make the groq/grok model string also overridable via `OPENROUTER_MODEL` env var. If the env var is set, use it instead of the hardcoded default.

    2. A `get_model(name: str) -> str` function that:
       - Looks up `name` in `MODEL_REGISTRY`
       - Raises `ValueError` with available model names if not found
       - Returns the Pydantic AI model string

    3. A `list_models() -> list[str]` function returning sorted list of friendly names.

    4. A `get_default_model() -> str` function that reads `default_model` from settings and returns the Pydantic AI model string.

    5. Log the resolved model name at import/call time so users can see which model is active (just a print statement for Phase 1 — Rich comes in Phase 2).

    The module should import from `codagent.config` to read settings.
  </action>
  <verify>
    <automated>cd /Users/kshitij/Documents/coding-agent-1 && python -c "from codagent.models import MODEL_REGISTRY, get_model, list_models; print(list_models()); print(get_model('gpt5')); print(get_model('claude')); print(get_model('groq'))"</automated>
  </verify>
  <done>MODEL_REGISTRY contains 3 providers, get_model() resolves friendly names to Pydantic AI strings, get_model('invalid') raises ValueError with available options, OpenRouter model overridable via env var</done>
</task>

</tasks>

<verification>
- `uv pip install -e .` succeeds without errors
- `python -c "from codagent.config import load_settings; s = load_settings(); assert s.mode == 'approval'"` passes
- `python -c "from codagent.models import get_model; assert 'openai:' in get_model('gpt5')"` passes
- `python -c "from codagent.models import get_model; get_model('invalid')"` raises ValueError
</verification>

<success_criteria>
- pyproject.toml exists with correct dependencies and entry point
- Package installs in development mode
- Settings load from environment with sensible defaults
- Model registry resolves all three providers
- No import errors when loading config or models modules
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-agent-loop/01-01-SUMMARY.md`
</output>
